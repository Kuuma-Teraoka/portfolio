　整理をもっと簡単にしたい。
kmoneyps a.csv a a
　とすると、a.csvファイルを書き換えて、中身を出力するというようにすればよい。しかし、プログラム自体はcsv形式の標準出力という形にして汎用性を保ち、シェルでなんとかする。
app/kmoney a.csv a a > tmp_kmoney
mv -i tmp_kmoney a.csv
echo"$ cat a.csv"
cat a.csv

　sedとロヂャースについては、書き換えは行いたくないので、
sed enav.csv > kmoney.csv

app/kmoney kmoney.csv ロヂャース ロヂャース >tmp_kmoney
mv -f tmp_kmoney kmoney.csv
echo "kmoney.csv has genelated"
echo "$cat kmoney.csv$
cat kmoney.csv
　


kmoneyEG enav.csv
　と入力すると、関数登録を呼び出し、

sed enav.csv > tmp
　で、金額、店名、日付に整理し、
app/kmoney tmp ロヂャース ロヂャース
　で、ロヂャースをまとめて、結果を標準出力する。

　kmoneyは、最初に全ての金額の合計を作っておき、ロヂャースを含まないものをそのまま出力、含むものを合計を作っておき、最後に出力。そしてさらに、すべての金額の合計を出力する。なお、この際合計金額:を含む場合はスキップするため、このプログラムは何度繰り返しても問題はない。ロヂャースを食費に、マルエツを食費に、食費を食費に、と言うようにしていけば整理していくことができる。










　家計簿プログラミング。
/app/rakuten a.csv ロジャース
ラーメン
サイゼ
ロジャースだけ合計

標準出力

　ロジャースを文字列に含むという正規表現に合わなければそのまま標準出力、あえば金額だけ変数にプラスして、最後にロジャースの合計として出力

　sed -i a.csv
で整理して、
　app/kmoney a.csv ﾛﾁﾞﾔｰｽ
で、最後の行に、sum,ﾛﾁﾞﾔｰｽ,15450
　を出力、これはなんかリダイレクトとかでもいいかも。
　文字列を引数に、文字の数、これはヌルコードの位置に等しい、を返すのと同じように、整数値と文字列の2つを引数に、その整数値が1だったら1番目のコンマの位置を返す関数を作る。

1234,だったら、01234の4に,があるので、4を返す。

　findが反応したら、次の処理をする。しなかったらそのまま標準出力。
　sumに料金を足す。料金については次の関数を考える。あるポインタと、整数値を引数に、そのポインタを含めた整数値の文を文字列と解釈して、それを数字に変換し、その数字を返す。

